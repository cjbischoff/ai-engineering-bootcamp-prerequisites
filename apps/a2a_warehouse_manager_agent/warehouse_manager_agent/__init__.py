"""Warehouse manager agent packaged for the A2A server (Week 6).

Submodules:

- ``agent`` — Google ADK :class:`~google.adk.agents.Agent` + LiteLLM + tools.
- ``agent_executor`` — A2A :class:`~a2a.server.agent_execution.AgentExecutor` bridge.
- ``tools`` — Postgres warehouse inventory helpers (same semantics as Week 5/6 DB).
"""
